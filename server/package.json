{
  "name": "chat-app",
  "version": "0.0.1",
  "description": "chat-app",
  "scripts": {
    "dev-run": "cross-env DEBUG=chat-app nodemon src/index.js --exec babel-node",
    "postinstall": "babel src -d dist --copy-files",
    "start": "cross-env DEBUG=chat-app node dist/index.js"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "cors": "^2.8.4",
    "debug": "^2.6.8",
    "express": "^4.15.3",
    "random-name": "^0.1.2",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "cross-env": "^5.0.5"
  }
}
